# Test Environment Configuration
# These values are used by Playwright E2E tests

# Base URL for tests
BASE_URL=http://localhost:8080

# Mock API credentials (used with MSW)
TEST_SERVER_URL=https://test-server.com
TEST_BEARER_TOKEN=valid-token

# Mapbox token (optional for tests)
VITE_MAPBOX_TOKEN=
